apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceestver
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    spec:
      containers:
      - name: aceestver
        image: dockerhub-username/aceestver:stable